services:
  face-api:
    build: .
    ports:
      - "8000:8000"
    environment:
      API_KEY: ${API_KEY:?set API_KEY in .env}
      MATCH_THRESHOLD: ${MATCH_THRESHOLD:-0.40}
    volumes:
      - ./data:/srv/data
    restart: unless-stopped
